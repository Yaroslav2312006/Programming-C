#include <stdio.h> — подключает стандартную библиотеку C, которая содержит функции для ввода и вывода (например, printf, scanf).
math.h - для математических функции(sqrt,...)
double calculate_perimeter(...) — объявляет функцию, которая будет вычислять периметр треугольника (сумму сторон a, b, c).
double calculate_area(...) — объявляет функцию, которая будет вычислять площадь треугольника по формуле Герона.
int main() — точка входа в программу. Выполнение кода начинается отсюда.
double a, b, c; — объявляет три переменные типа double (числа с плавающей точкой двойной точности), которые будут хранить длины сторон треугольника.
printf(...) — выводит сообщение с просьбой ввести три числа.
scanf("%lf %lf %lf", &a, &b, &c) — считывает три числа типа double и сохраняет их в переменные a, b, c.
if (a + b > c && a + c > b && b + c > a) — проверяет, можно ли из этих сторон построить треугольник (сумма любых двух сторон должна быть больше третьей).
calculate_perimeter(a, b, c) — вызывает функцию, которая вычисляет периметр (a + b + c).
calculate_area(a, b, c) — вызывает функцию, которая вычисляет площадь по формуле Герона.
%.2lf — выводит число с плавающей точкой с округлением до 2 знаков после запятой.
\n — перенос строки.
return 0; — означает успешное завершение программы.
sqrt — функция из <math.h>, вычисляющая квадратный корень.
calculate_perimeter() Принимает три параметра типа double (стороны треугольника), Возвращает сумму этих параметров (периметр треугольника) ,Простая операция сложения трех чисел
